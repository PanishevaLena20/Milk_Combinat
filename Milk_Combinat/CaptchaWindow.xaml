<Window x:Class="Milk_Combinat.CaptchaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Milk_Combinat"
        mc:Ignorable="d"
        Title="CaptchaWindow" Height="450" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Соберите правильный порядок: 1, 2, 3, 4" 
                   FontSize="16" FontWeight="Bold" Foreground="#2C3E50" 
                   HorizontalAlignment="Center" Margin="0,0,0,15"/>

        <Grid x:Name="puzzleGrid" Grid.Row="1" Margin="0,0,0,15">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
        </Grid>

        <TextBlock x:Name="lblResult" Grid.Row="2" Text="" 
                   FontSize="14" TextAlignment="Center" Margin="0,10,0,0"/>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button x:Name="btnCheck" Content="✅ Проверить" Width="120" Height="40" 
                    Margin="10" Background="#4CAF50" Foreground="White" 
                    FontWeight="Bold" Click="btnCheck_Click_1"/>
            <Button x:Name="btnShuffle" Content="🔄 Перемешать" Width="120" Height="40" 
                    Margin="10" Background="#FF9800" Foreground="White" 
                    FontWeight="Bold" Click="btnShuffle_Click_1"/>
        </StackPanel>
    </Grid>
</Window>